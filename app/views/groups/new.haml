- @title = t '.title'
- @side_type = :community
.summary
  = t '.summary'
- form_for(@group) do |f|
  = f.error_messages
  .row
    = f.label :title
    = f.text_field :title
  %span.submit
    = f.submit t('.submit')
